const config = {
  appId: 'org.thronoschain.medice',
  appName: 'ThronomedICE',
  webDir: '.next',
  server: {
    url: 'https://medice.thronoschain.org',
    cleartext: false,
  },
} as const;

export default config;
